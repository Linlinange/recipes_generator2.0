[project]
name = "recipe_generator2"
version = "2.1.0"
requires-python = ">=3.7"
dependencies = ["pydantic"]

[project.optional-dependencies]
tui = ["textual"]
gui = ["flet>=0.21.0"]
dev = ["pytest", "black", "ruff"]